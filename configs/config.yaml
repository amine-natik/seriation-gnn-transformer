hydra:
  run:
    dir: .outputs/${hydra.job.name}/data_${data}_model_${model}

data: 'Cora'
data_root: './data/'
exp_name: ???
save_dir: '.outputs'
process_method: 'label_ordering'
model: 'RawTransformer'
loss_fn: 'NLLLoss'
optimizer: 'Adam'
device: 'cuda'
seed: 3
d_model: 128
dim_feedforward: 128
n_head: 4
num_layers: 1
num_epochs: 10
show_log: true
save_ckpt: false
dropout: 0.2
lr: 0.01
idx: 82
